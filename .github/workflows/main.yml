name: Create APK from Master

on:
  workflow_dispatch:
  push:
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
  
    - name: Echo
      run: echo Testing ${{ github.event_name }} ${{ github.event.pusher.name }}
